{
  "name": "router-adapter",
  "repository": {
    "url": "git+https://github.com/saulogt/router-adapter.git",
    "type": "git"
  },
  "version": "0.1.1",
  "description": "The main goal of this package is to separate the http from the logic creating a simple link between the router and the controller",
  "main": "dist/index.js",
  "tipings": "dist/index",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepublish": "tsc"
    
  },
  "author": "Saulo Tauil",
  "license": "ISC",
  "devDependencies": {
    "express": "^4.16.2",
    "typescript": "~2.5.3"
  }
}
